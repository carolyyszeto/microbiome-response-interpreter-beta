interface:
  display_name: "Microbiome Response Interpreter V6"
  short_description: "Audit microbiome tables and standardized geometry-ready bundles, route conservatively, and run backend response-geometry summaries when supported."
  default_prompt: "Use $microbiome-response-interpreter-v6 to audit this microbiome table or standardized geometry-ready bundle, choose the safest conservative workflow, and run the relevant backend outputs or readable summaries if the inputs support that path."

policy:
  allow_implicit_invocation: true
