agent_config:
  display_name: "Microbiome Response Interpreter V6.5"
  description: "Audit microbiome tables and standardized geometry-ready bundles, route conservatively, and run backend response-geometry summaries when supported."
  model_name: "gemini-1.5-pro"
  temperature: 0.2
  system_instruction: |
    Use $microbiome-response-interpreter-v6-5 to audit this microbiome table or standardized geometry-ready bundle, choose the safest conservative workflow, and run the relevant backend outputs or readable summaries if the inputs support that path.
  tools:
    - function_declarations:
        - name: "run_response_geometry"
          description: "Executes backend response-geometry summaries and metrics."
